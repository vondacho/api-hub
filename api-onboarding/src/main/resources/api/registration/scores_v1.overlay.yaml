overlay: 1.0.0
info:
  title: Overlay for injecting scoring data into the API document
  version: v1
extends: '/registration_v1_openapi.yaml'
actions:
  - target: '$' # Root of document
    update:
      info:
        x-score: {{score.global.evaluation}}
