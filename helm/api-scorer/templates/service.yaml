apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-scorer.fullname" . }}
  labels:
    {{- include "api-scorer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-scorer.selectorLabels" . | nindent 4 }}
