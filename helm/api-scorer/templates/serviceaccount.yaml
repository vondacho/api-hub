{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "api-scorer.serviceAccountName" . }}
  labels:
    {{- include "api-scorer.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The scorer does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
