apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-registry.fullname" . }}
  labels:
    {{- include "api-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-registry.selectorLabels" . | nindent 4 }}
