{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "api-onboarding.serviceAccountName" . }}
  labels:
    {{- include "api-onboarding.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The service does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
