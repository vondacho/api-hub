apiVersion: v2
name: api-onboarding
description: API contract onboarding service — parse, validate, score and register API contracts
type: application

# Chart version. Bump on every change to the chart itself.
version: 0.1.0

# Version of api-onboarding shipped by default. Matches <version> in api-onboarding/pom.xml
# and is used as the default image tag.
appVersion: "1.0-SNAPSHOT"

keywords:
  - openapi
  - asyncapi
  - api-governance
home: https://github.com/obya/my-api-portal
sources:
  - https://github.com/obya/my-api-portal/tree/main/api-onboarding
maintainers:
  - name: Olivier von Dach
