apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-registry-db.fullname" . }}
  labels:
    {{- include "api-registry-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "api-registry-db.selectorLabels" . | nindent 4 }}
